---
- name: Molecule.tests.roles.deploy-finish | Main
  hosts: localhost
  gather_facts: true

  tasks:
    - name: Molecule.tests.roles.deploy-finish | Main | Include all tests
      ansible.builtin.include_tasks: "{{ item }}"
      with_fileglob:
        - variables/*.yml
