---
- name: Molecule | Tests | Variables
  hosts: localhost
  gather_facts: false

  tasks:
    - name: Molecule | Tests | Variables | Include all tests
      ansible.builtin.include_tasks: "{{ item }}"
      with_fileglob:
        - asserts/*.yml
