import { FC } from 'react';
import OperationsTable from '@widgets/operations-table';
import { Box } from '@mui/material';

const Operations: FC = () => {
  return (
    <Box>
      <OperationsTable />
    </Box>
  );
};

export default Operations;
